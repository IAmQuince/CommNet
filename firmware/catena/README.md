# Catena firmware reference

This folder contains the uploaded `commnet-catena-bridge` firmware reference package as received for review.

CommNet treats this as a serial-first Catena bridge, not Meshtastic firmware. RF delivery is not claimed unless firmware and receiver tests produce RX/REMOTE_ACK evidence.
