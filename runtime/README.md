# Runtime folder

CommNet writes mutable local state here when launched.

Fresh package archives intentionally do not include a pre-populated runtime database. Smoke-test reports are copied to `proof/` before packaging.
